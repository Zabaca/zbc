{
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "lib": ["esnext"],
    "types": ["@cloudflare/workers-types"]
  },
  "include": ["worker"],
  "// exclude": "Test files are excluded because bun:test's `expect` typings clash with @cloudflare/workers-types' Request/Response shapes when both are in one program (same split warehouse's tsconfig.worker.json documents). `bun test` still type-strips and runs them.",
  "exclude": ["node_modules", "**/*.test.ts"]
}
